# Things to Change

- the attribute in the trip (priceOfTrip) is need to change to tripPrice
- In trip model change the isComplete attribute to status and in frontend add the variations for cancel, pending and completed and progeress status
  -in Trip tab change the status quantity in which add 0 to number where number is one like 1 make it 01 to make consistend ui

# Things need to add

- In trip model add the attribute of deadline and add it to ui
-
